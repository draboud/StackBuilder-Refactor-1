import { _activateStateComp } from "../model";

export default class View {
  _data;
  _activeStateComp;
  _activeCompBlock;

  // _retarget = function (comp, id) {
  //   switch (comp){
  //     case _activateStateComp
  //   }
  //   _activateStateComp = stateData.stateCompsArray.find(
  //     (el) => el.id === stateData.activeId
  //   );
  // };
}
